/**
 *
 * create by ligx
 *
 * @flow
 */
import * as React from 'react';
import Menu from './';

const { MenuItem, } = Menu;
export default () => {
  return <Menu>
    <MenuItem>
      a
    </MenuItem>
    <MenuItem>
      b
    </MenuItem>
  </Menu>;
};
