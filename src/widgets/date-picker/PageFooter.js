import React, { Component } from 'react';
import { Footer } from './styled';
class PageFooter extends Component<preProps, preState> {
  render() {
    return <Footer>sds</Footer>;
  }
}
export default PageFooter;
